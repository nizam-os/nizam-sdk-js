// This file was auto-generated by Fern from our API Definition.

/**
 * A revocable, read-only link to a conversation.
 */
export interface ConversationShare {
    /** Share id. Not a credential — safe to log and to use in owner-facing URLs. */
    id: string;
    /** The conversation this link exposes. */
    conversation_id: string;
    /** The link credential — RETURNED ONLY ON CREATION, and never again. Store it when you receive it; it cannot be recovered. */
    token?: string | undefined;
    /** How far the link reaches. */
    audience: ConversationShare.Audience;
    /** Lifecycle status. */
    status: ConversationShare.Status;
    /** When the link stops working, or null for no expiry. */
    expires_at?: string | undefined;
    /** Creation timestamp. */
    created_at: string;
    /** Statuses this link may move to next, derived from `status`. Revocation is one-way. */
    allowed_transitions: ConversationShare.AllowedTransitions.Item[];
    /** Object type discriminator. */
    object: ConversationShare.Object_;
}

export namespace ConversationShare {
    /** How far the link reaches. */
    export const Audience = {
        Workspace: "workspace",
        Public: "public",
    } as const;
    export type Audience = (typeof Audience)[keyof typeof Audience];
    /** Lifecycle status. */
    export const Status = {
        Active: "active",
        Revoked: "revoked",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    export type AllowedTransitions = AllowedTransitions.Item[];

    export namespace AllowedTransitions {
        /** A legal next status. */
        export const Item = {
            Active: "active",
            Revoked: "revoked",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }

    /** Object type discriminator. */
    export const Object_ = {
        ConversationShare: "conversation_share",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
