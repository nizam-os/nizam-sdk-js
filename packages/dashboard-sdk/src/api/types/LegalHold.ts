// This file was auto-generated by Fern from our API Definition.

import type * as NizamDashboard from "../index.js";

/**
 * A preservation order that defers erasure of the data it covers.
 */
export interface LegalHold {
    /** Hold id. */
    id: string;
    /** The organization whose data is preserved. */
    organization_id: string;
    target: NizamDashboard.LegalHoldTarget;
    /** Why the hold was placed. */
    reason: string;
    /** When the hold was released, or null while it is still in force. */
    lifted_at?: string | undefined;
    /** Why the hold was released, or null while it is still in force. */
    lifted_reason?: string | undefined;
    /** When the hold was placed. */
    created_at: string;
    object: LegalHold.Object_;
}

export namespace LegalHold {
    export const Object_ = {
        AssistantLegalHold: "assistant_legal_hold",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
