// This file was auto-generated by Fern from our API Definition.

import type * as NizamDashboard from "../index.js";

/**
 * Purchased and promotional AI credits, separate from the plan's included period allowance. Purchased credits never expire and carry across billing periods, plan changes and cancellation.
 */
export interface CreditBalance {
    /** Credits available to spend right now. */
    spendable: number;
    /** Credits held but not currently spendable: frozen by a payment dispute, or suspended while the workspace has no active plan. Never silently dropped: held is not the same as gone. */
    suspended: number;
    /** The grants behind the totals, newest first. */
    lots: NizamDashboard.CreditLot[];
    object: CreditBalance.Object_;
}

export namespace CreditBalance {
    export const Object_ = {
        CreditBalance: "credit_balance",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
