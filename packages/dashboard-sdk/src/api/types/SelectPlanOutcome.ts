// This file was auto-generated by Fern from our API Definition.

import type * as NizamDashboard from "../index.js";

/**
 * The result of selecting a plan. The outcome discriminator selects the variant: `applied` means the change is already in effect; `card_required` means nothing has changed yet and the client must confirm the returned setup session in the embedded payment component (saving a card, charging nothing), then call select-plan again with the confirmed `checkout_session_id` to complete the selection.
 */
export type SelectPlanOutcome =
    | NizamDashboard.SelectPlanOutcome.Applied
    | NizamDashboard.SelectPlanOutcome.CardRequired;

export namespace SelectPlanOutcome {
    export interface Applied extends NizamDashboard.SelectPlanApplied {
        outcome: "applied";
    }

    export interface CardRequired extends NizamDashboard.SelectPlanCardRequired {
        outcome: "card_required";
    }
}
