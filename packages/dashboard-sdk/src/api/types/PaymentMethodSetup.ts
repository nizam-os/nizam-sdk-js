// This file was auto-generated by Fern from our API Definition.

/**
 * A payment-method setup session to confirm in the embedded payment component. After the confirmation succeeds, submit the session id back via `PUT /v1/billing/payment-method`.
 */
export interface PaymentMethodSetup {
    /** The session confirmation secret for the embedded payment component. */
    client_secret: string;
    /** The payment processor's publishable (client-side) key to initialize the embedded payment component with. Always paired with the client_secret. */
    publishable_key: string;
    object: PaymentMethodSetup.Object_;
}

export namespace PaymentMethodSetup {
    export const Object_ = {
        PaymentMethodSetup: "payment_method_setup",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
