// This file was auto-generated by Fern from our API Definition.

import type * as NizamDashboard from "../index.js";

/**
 * The organization's effective entitlements: capability flags, every enforced allowance with its current draw, and the window the metered allowances reset on. These are the EFFECTIVE values — the plan's grants merged with every active add-on and zeroed when no agreement grants — so prefer them over a plan's published `features` for any gating decision. The window is the span the current grant covers: the billing period once subscribed, the trial's own span during a card-less trial. Both bounds are null when nothing is enforceable yet (no live agreement, or one still provisioning); the allowances still report, so a workspace with no plan sees its zeroed ceilings rather than an empty screen.
 */
export interface Entitlements {
    /** Every capability the catalog can grant, resolved for this organization. Reported in full — an ungranted capability is present and false, not absent — so a client can render it as a locked upsell without knowing the catalog. */
    flags: Record<string, boolean>;
    /** The allowances, metered first, each group alphabetical by name. */
    allowances: NizamDashboard.Allowance[];
    /** Start of the window the metered allowances accumulate over. */
    window_start?: string | undefined;
    /** When the metered allowances reset. Null when nothing is enforceable yet. */
    window_end?: string | undefined;
    object: Entitlements.Object_;
}

export namespace Entitlements {
    export const Object_ = {
        Entitlements: "entitlements",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
