// This file was auto-generated by Fern from our API Definition.

/**
 * A quota ceiling and the organization's current draw against it. `limit` is null when the allowance is unlimited (no ceiling in force) and 0 when it is hard-blocked — never treat the two alike. `used` may exceed `limit` (soft meters allow overage, dunning grace allows it temporarily, and a downgrade can lower a ceiling under consumption already spent); `remaining` floors at zero.
 */
export interface Allowance {
    /** The quota key this allowance caps. */
    key: string;
    /** Display name for the allowance. */
    name: string;
    /** Singular unit the counts are expressed in, when the allowance has one. */
    unit?: string | undefined;
    /** How the ceiling is measured. `metered` accumulates over the window and resets with it; `standing` is a live count that never resets. */
    kind: Allowance.Kind;
    /** The ceiling in force, or null when unlimited. */
    limit?: number | undefined;
    /** How much of the allowance is drawn right now. */
    used: number;
    /** Ceiling minus usage, floored at zero; null when unlimited. */
    remaining?: number | undefined;
}

export namespace Allowance {
    /** How the ceiling is measured. `metered` accumulates over the window and resets with it; `standing` is a live count that never resets. */
    export const Kind = {
        Metered: "metered",
        Standing: "standing",
    } as const;
    export type Kind = (typeof Kind)[keyof typeof Kind];
}
