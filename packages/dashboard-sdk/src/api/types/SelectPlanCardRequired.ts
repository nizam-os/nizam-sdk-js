// This file was auto-generated by Fern from our API Definition.

import type * as NizamDashboard from "../index.js";

/**
 * A verified card must be on file first. Nothing has changed; confirm the setup session with the embedded payment component using `client_secret` (this saves the card — it charges nothing), then call select-plan again with the confirmed session's `checkout_session_id` to complete the selection: a trial starts immediately, or the subscription is created and its first invoice is charged to the saved card.
 */
export interface SelectPlanCardRequired {
    subscription?: NizamDashboard.PlatformSubscription | undefined;
    /** The setup-mode checkout session's client secret the embedded checkout initializes with and confirms the card against. */
    client_secret: string;
    /** The payment processor's publishable (client-side) key to initialize the embedded payment component with. Always paired with the client_secret. */
    publishable_key: string;
    object: SelectPlanCardRequired.Object_;
}

export namespace SelectPlanCardRequired {
    export const Object_ = {
        SelectPlanOutcome: "select_plan_outcome",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
