// This file was auto-generated by Fern from our API Definition.

/**
 * Whether the organization can still start a free trial. One trial per organization, ever: any prior agreement, live or ended, consumes it. Plans carry their own `trial_days`; a selection starts a trial only when the organization is eligible AND the plan offers one.
 */
export interface TrialEligibility {
    /** True while the organization has never had any subscription agreement. */
    eligible: boolean;
    object: TrialEligibility.Object_;
}

export namespace TrialEligibility {
    export const Object_ = {
        TrialEligibility: "trial_eligibility",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
