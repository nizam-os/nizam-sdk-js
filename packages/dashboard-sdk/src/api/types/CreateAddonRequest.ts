// This file was auto-generated by Fern from our API Definition.

import type * as NizamDashboard from "../index.js";

/**
 * Author a new add-on in the catalog.
 */
export interface CreateAddonRequest {
    /** Stable add-on code (natural key) — a lowercase url-safe slug. */
    code: string;
    /** Display name. */
    name: string;
    /** Catalog category. */
    category: CreateAddonRequest.Category;
    /** Pricing shape. */
    pricing_model: CreateAddonRequest.PricingModel;
    /** Price in the currency's minor units (omit for a free add-on). */
    price_minor_units?: number | undefined;
    /** ISO-4217 currency code (required when priced). */
    currency?: string | undefined;
    /** Billing cadence (required for a recurring model; omit otherwise). */
    billing_period?: CreateAddonRequest.BillingPeriod | undefined;
    /** Description. */
    description?: string | undefined;
    /** Marketing copy for the storefront card. */
    marketing_text?: string | undefined;
    /** Storefront icon file id. */
    icon_file_id?: string | undefined;
    features?: NizamDashboard.AddonFeaturesRequest | undefined;
    /** Whether a tenant may self-subscribe (default true). */
    self_serve?: boolean | undefined;
}

export namespace CreateAddonRequest {
    /** Catalog category. */
    export const Category = {
        Analytics: "analytics",
        Integrations: "integrations",
        Compliance: "compliance",
        Capacity: "capacity",
        Automation: "automation",
        Telematics: "telematics",
        Communications: "communications",
        Storage: "storage",
        Security: "security",
        Support: "support",
        Branding: "branding",
        WhiteLabel: "white_label",
        ApiAccess: "api_access",
        Onboarding: "onboarding",
        Other: "other",
    } as const;
    export type Category = (typeof Category)[keyof typeof Category];
    /** Pricing shape. */
    export const PricingModel = {
        Free: "free",
        Flat: "flat",
        PerSeat: "per_seat",
        Usage: "usage",
        Tiered: "tiered",
        OneTime: "one_time",
    } as const;
    export type PricingModel = (typeof PricingModel)[keyof typeof PricingModel];
    /** Billing cadence (required for a recurring model; omit otherwise). */
    export const BillingPeriod = {
        Weekly: "weekly",
        Biweekly: "biweekly",
        Monthly: "monthly",
        Quarterly: "quarterly",
        Semiannual: "semiannual",
        Yearly: "yearly",
    } as const;
    export type BillingPeriod = (typeof BillingPeriod)[keyof typeof BillingPeriod];
}
