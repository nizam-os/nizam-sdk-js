// This file was auto-generated by Fern from our API Definition.

/**
 * One grant of AI credits and what remains of it.
 */
export interface CreditLot {
    /** Where the credits came from. */
    kind: CreditLot.Kind;
    /** Current state of the grant. */
    status: CreditLot.Status;
    /** Credits originally granted. */
    granted: number;
    /** Credits still unspent. */
    remaining: number;
    /** When these credits lapse. Always null for purchased credits — they do not expire. */
    expires_at?: string | undefined;
    /** When the grant was made. */
    granted_at: string;
}

export namespace CreditLot {
    /** Where the credits came from. */
    export const Kind = {
        Purchased: "purchased",
        Promotional: "promotional",
    } as const;
    export type Kind = (typeof Kind)[keyof typeof Kind];
    /** Current state of the grant. */
    export const Status = {
        Available: "available",
        Depleted: "depleted",
        Expired: "expired",
        Frozen: "frozen",
        Revoked: "revoked",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
}
