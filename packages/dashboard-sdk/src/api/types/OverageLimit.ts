// This file was auto-generated by Fern from our API Definition.

import type * as NizamDashboard from "../index.js";

/**
 * An organization's self-imposed ceiling on metered overage spend per billing period. While uncapped, usage past a plan's included allowance is billed rather than refused; once the cap is reached, further metered usage is refused with `quota.spend_cap_reached` until the cap is raised.
 */
export interface OverageLimit {
    monthly_cap?: NizamDashboard.Money | undefined;
    /** The currency the organization is billed in. */
    currency: string;
    object: OverageLimit.Object_;
}

export namespace OverageLimit {
    export const Object_ = {
        OverageLimit: "overage_limit",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
