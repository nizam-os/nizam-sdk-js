// This file was auto-generated by Fern from our API Definition.

import type * as NizamDashboard from "../index.js";

/**
 * A conversation the search found, and why.
 */
export interface AssistantSearchHit {
    /** Conversation id — open it directly. */
    conversation_id: string;
    /** The conversation's title, or null while it is still unnamed. */
    title?: string | undefined;
    /** Lifecycle status — an archived thread is still findable, and says so. */
    status: AssistantSearchHit.Status;
    /** The matched text with `<mark>` around the terms that matched — the title when the thread matched by name, otherwise the passage inside it that did. One fragment, at most twenty words. */
    snippet: string;
    match: NizamDashboard.AssistantSearchMatch;
    /** When the conversation last changed. */
    updated_at: string;
    object: AssistantSearchHit.Object_;
}

export namespace AssistantSearchHit {
    /** Lifecycle status — an archived thread is still findable, and says so. */
    export const Status = {
        Active: "active",
        Archived: "archived",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    export const Object_ = {
        AssistantSearchHit: "assistant_search_hit",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
