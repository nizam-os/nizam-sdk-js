// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         operatorId: "00000000-0000-0000-0000-000000000000",
 *         serialNumber: "<string>",
 *         reason: "key_compromise"
 *     }
 *
 * @example
 *     {
 *         operatorId: "00000000-0000-0000-0000-000000000000",
 *         serialNumber: "<string>",
 *         reason: "cessation_of_operation"
 *     }
 */
export interface RevokeMachineCertificateRequest {
    operatorId: string;
    serialNumber: string;
    /** RFC 5280 revocation reason. `key_compromise` says the private key is in someone else's hands and CASCADES to every other certificate this machine holds, because a leaked key indicts the device rather than one credential of it. The others touch only the certificate named. Defaults to `unspecified`. */
    reason?: RevokeMachineCertificateRequest.Reason;
}

export namespace RevokeMachineCertificateRequest {
    /** RFC 5280 revocation reason. `key_compromise` says the private key is in someone else's hands and CASCADES to every other certificate this machine holds, because a leaked key indicts the device rather than one credential of it. The others touch only the certificate named. Defaults to `unspecified`. */
    export const Reason = {
        Unspecified: "unspecified",
        KeyCompromise: "key_compromise",
        Superseded: "superseded",
        CessationOfOperation: "cessation_of_operation",
        PrivilegeWithdrawn: "privilege_withdrawn",
    } as const;
    export type Reason = (typeof Reason)[keyof typeof Reason];
}
