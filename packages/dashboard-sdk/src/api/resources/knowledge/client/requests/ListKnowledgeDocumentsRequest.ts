// This file was auto-generated by Fern from our API Definition.

import type * as NizamDashboard from "../../../../index.js";

/**
 * @example
 *     {
 *         collection_id: "00000000-0000-0000-0000-000000000000",
 *         q: "<string>",
 *         starting_after: "Y3Vyc29yX25leHRfMDFKNVE=",
 *         ending_before: "Y3Vyc29yX25leHRfMDFKNVE="
 *     }
 */
export interface ListKnowledgeDocumentsRequest {
    /** Filter to a single collection. */
    collection_id?: string;
    /** Filter by ingestion status. Comma-separated; matched case-insensitively. */
    status?:
        | NizamDashboard.ListKnowledgeDocumentsRequestStatusItem
        | NizamDashboard.ListKnowledgeDocumentsRequestStatusItem[];
    /** Filter by source. Comma-separated; matched case-insensitively. */
    source?:
        | NizamDashboard.ListKnowledgeDocumentsRequestSourceItem
        | NizamDashboard.ListKnowledgeDocumentsRequestSourceItem[];
    /** Free-text search over the document title and its collection name. Narrows the page; the sort still governs the order. */
    q?: string;
    /** Sort order. Allowed fields: `created_at`, `updated_at`, `title`, `collection`, `status`, `source`, `size`. Append `:asc` or `:desc` to each field (direction required, e.g. `created_at:desc`), comma-separated for multi-sort; unknown fields or directions are rejected with `400 validation_failed`. */
    sort?:
        | NizamDashboard.ListKnowledgeDocumentsRequestSortItem
        | NizamDashboard.ListKnowledgeDocumentsRequestSortItem[];
    /** Inline related resources instead of returning id-only references. Allowed: `collection`. Repeat the parameter (`expand[]=a&expand[]=b`) to inline several; dot-notation chains a nested hop (max depth 4). An unknown or unpermitted value is rejected with `400 validation_failed`. Unexpanded relations stay id-only and the nested object is omitted from the response. */
    "expand[]"?:
        | NizamDashboard.ListKnowledgeDocumentsRequestExpandItem
        | NizamDashboard.ListKnowledgeDocumentsRequestExpandItem[];
    /** Page size. Default 20, maximum 100. Out-of-range values are silently clamped; the response body's `limit` field reflects what was applied. */
    limit?: number;
    /** Opaque cursor — return the page starting strictly after this entity in the sort order. Mutually exclusive with `ending_before`. */
    starting_after?: string;
    /** Opaque cursor — return the page ending strictly before this entity in the sort order. Mutually exclusive with `starting_after`. */
    ending_before?: string;
}
