// This file was auto-generated by Fern from our API Definition.

/**
 * Create an artifact in a conversation, at version 1.
 */
export interface CreateArtifactRequest {
    /** What kind of thing this is. */
    type: CreateArtifactRequest.Type;
    /** Display name. */
    title: string;
    /** Syntax hint for the editor — the source language of a `code` artifact or the dialect of a `diagram` one. Omit for a `document`. */
    language?: string | undefined;
    /** The artifact's initial content. */
    content: string;
    /** The assistant message whose turn produced this, when a model did. Omit when a member is creating the artifact themselves. */
    created_by_message_id?: string | undefined;
}

export namespace CreateArtifactRequest {
    /** What kind of thing this is. */
    export const Type = {
        Document: "document",
        Code: "code",
        Diagram: "diagram",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
}
