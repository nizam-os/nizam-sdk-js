// This file was auto-generated by Fern from our API Definition.

/**
 * A certificate issued to an autonomous operator.
 */
export interface WorkloadCredential {
    /** Certificate serial number, uppercase hexadecimal. The identifier a revocation list names and the one to quote when revoking. */
    serial_number: string;
    /** The issued certificate, PEM-encoded. */
    certificate_pem: string;
    /** The issuing chain, issuer-first and excluding the leaf: the intermediate that signed this certificate, then the root. Concatenate it after `certificate_pem` to present a complete chain. */
    chain_pem: string[];
    /** Not valid before this instant. */
    not_before: string;
    /** Not valid after this instant. Renew well before it — renewal leaves this certificate valid until it lapses, so a rotation that fails halfway cannot strand the machine. */
    not_after: string;
    /** Object type discriminator. */
    object: WorkloadCredential.Object_;
}

export namespace WorkloadCredential {
    /** Object type discriminator. */
    export const Object_ = {
        WorkloadCredential: "workload_credential",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
