// This file was auto-generated by Fern from our API Definition.

/**
 * Require SSO for members, naming the break-glass admin.
 */
export interface RequireSsoRequest {
    /** User id of the exempt break-glass account. Must be an active admin member of the organization — the exemption grants no new authority. */
    break_glass_user_id: string;
    /** Also require SSO for members whose only role is operator (driver). Off by default: most identity providers hold no driver accounts, and enforcing them would lock the driver workforce out. Turn on only when drivers genuinely sign in through your identity provider. */
    applies_to_operators?: boolean | undefined;
}
