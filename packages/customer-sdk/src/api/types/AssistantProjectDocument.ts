// This file was auto-generated by Fern from our API Definition.

/**
 * A knowledge document attached to a project as standing context.
 */
export interface AssistantProjectDocument {
    /** Knowledge document id. */
    document_id: string;
    /** Document title, or null when the document has since been removed from the corpus (a stale attachment the member can detach). */
    title?: string | undefined;
    /** The document's ingestion status, or null when it is no longer in the corpus. Only an indexed document can actually ground a turn. */
    status?: AssistantProjectDocument.Status | undefined;
    /** What this document weighs in prompt tokens, or null when it has no live indexed content yet — which is not the same as weighing nothing. */
    tokens?: number | undefined;
    /** Who attached it. */
    attached_by?: string | undefined;
    /** When it was attached. */
    attached_at: string;
    /** Object type discriminator. */
    object: AssistantProjectDocument.Object_;
}

export namespace AssistantProjectDocument {
    /** The document's ingestion status, or null when it is no longer in the corpus. Only an indexed document can actually ground a turn. */
    export const Status = {
        Pending: "pending",
        Extracting: "extracting",
        Chunking: "chunking",
        Indexed: "indexed",
        Failed: "failed",
        Archived: "archived",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /** Object type discriminator. */
    export const Object_ = {
        AssistantProjectDocument: "assistant_project_document",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
