// This file was auto-generated by Fern from our API Definition.

/**
 * One immutable snapshot of an artifact's content. Versions are append-only: a restore adds a new version cloning an older one rather than removing anything.
 */
export interface ArtifactVersion {
    /** Stable UUID. */
    id: string;
    /** 1-based version number, unique within the artifact. */
    version_number: number;
    /** Character length of this version's content. */
    content_length: number;
    /** The assistant message whose turn generated this version, when a model did. `null` when a member saved it themselves. */
    created_by_message_id?: string | undefined;
    /** Set when this version was produced by restoring an older one: the version it was cloned from. `null` for an ordinary save. */
    restored_from_version_id?: string | undefined;
    /** Who authored this version. The zero UUID is the platform itself, for a model-generated version. */
    author_id: string;
    /** When the version was written. */
    created_at: string;
    /** Object type discriminator. */
    object: ArtifactVersion.Object_;
}

export namespace ArtifactVersion {
    /** Object type discriminator. */
    export const Object_ = {
        ArtifactVersion: "artifact_version",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
