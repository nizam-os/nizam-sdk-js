// This file was auto-generated by Fern from our API Definition.

/**
 * Values the administrator registers at their identity provider. Available from the draft stage on.
 */
export interface SsoSetupDetails {
    /** OIDC redirect URI / SAML assertion consumer service (ACS) URL. */
    redirect_uri?: string | undefined;
    /** SAML service-provider entity id. */
    sp_entity_id?: string | undefined;
    /** SAML SP metadata document URL. Null until the provider is first configured (the identity system cannot render it before the provider exists). */
    sp_metadata_url?: string | undefined;
    /** The claim (OIDC) / attribute (SAML) name Nizam reads asserted directory groups from. */
    groups_claim?: string | undefined;
}
