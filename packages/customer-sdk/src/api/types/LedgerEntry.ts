// This file was auto-generated by Fern from our API Definition.

import type * as NizamCustomer from "../index.js";

/**
 * A single immutable debit or credit line of the double-entry journal.
 */
export interface LedgerEntry {
    /** Unique identifier of the entry. */
    id: string;
    /** Groups the balanced set of lines that form one posting. */
    transaction_id: string;
    /** The account this line posts against. */
    account_id: string;
    /** The account's chart code. */
    account_code: string;
    /** Whether the line debits or credits the account. */
    direction: LedgerEntry.Direction;
    amount: NizamCustomer.Money;
    /** The kind of document that produced this line. */
    source_type: LedgerEntry.SourceType;
    /** The id of the source document (null for a manual adjustment). */
    source_id?: string | undefined;
    /** Free-text line memo. */
    description?: string | undefined;
    /** When the entry takes economic effect. */
    posted_at: string;
    /** When the entry row was written. */
    created_at: string;
    object: LedgerEntry.Object_;
}

export namespace LedgerEntry {
    /** Whether the line debits or credits the account. */
    export const Direction = {
        Debit: "debit",
        Credit: "credit",
    } as const;
    export type Direction = (typeof Direction)[keyof typeof Direction];
    /** The kind of document that produced this line. */
    export const SourceType = {
        Invoice: "invoice",
        Payment: "payment",
        CodCollection: "cod_collection",
        MerchantPayout: "merchant_payout",
        CreditNote: "credit_note",
        Refund: "refund",
        Reversal: "reversal",
    } as const;
    export type SourceType = (typeof SourceType)[keyof typeof SourceType];
    export const Object_ = {
        LedgerEntry: "ledger_entry",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
