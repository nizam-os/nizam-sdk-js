// This file was auto-generated by Fern from our API Definition.

/**
 * A folder that groups assistant conversations.
 */
export interface AssistantProject {
    /** Project id. */
    id: string;
    /** Display name. */
    name: string;
    /** What the project collects, or null. */
    description?: string | undefined;
    /** The standing brief every conversation filed into this project converses under. Present on a single-project read; always null on the list read, which does not select it. */
    instructions?: string | undefined;
    /** How many live conversations are filed into it. Present on the list read; null on a single-project read, which does not compute it. */
    conversation_count?: number | undefined;
    /** Creation timestamp. */
    created_at: string;
    /** Last-updated timestamp. */
    updated_at: string;
    /** Object type discriminator. */
    object: AssistantProject.Object_;
}

export namespace AssistantProject {
    /** Object type discriminator. */
    export const Object_ = {
        AssistantProject: "assistant_project",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
