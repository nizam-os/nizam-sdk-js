// This file was auto-generated by Fern from our API Definition.

import type * as NizamCustomer from "../index.js";

/**
 * A first-party paid add-on in the platform billing catalog (admin view).
 */
export interface Addon {
    /** Stable add-on id. */
    id: string;
    /** Stable add-on code (natural key). */
    code: string;
    /** Display name. */
    name: string;
    /** Description. */
    description?: string | undefined;
    /** Marketing copy for the storefront card. */
    marketing_text?: string | undefined;
    /** Storefront icon file id. */
    icon_file_id?: string | undefined;
    /** Catalog category. */
    category: Addon.Category;
    /** Pricing shape. */
    pricing_model: Addon.PricingModel;
    price?: NizamCustomer.Money | undefined;
    /** Billing cadence (null for free / one-time). */
    billing_period?: Addon.BillingPeriod | undefined;
    features: NizamCustomer.AddonFeatures;
    /** Processor (Stripe) Product id, once provisioned. */
    processor_product_id?: string | undefined;
    /** Processor (Stripe) Price id, once provisioned. */
    processor_price_id?: string | undefined;
    /** Whether the add-on is sellable. */
    active: boolean;
    /** Whether a tenant may self-subscribe (vs admin / sales-assisted). */
    self_serve: boolean;
    /** Creation time. */
    created_at: string;
    /** Last update time. */
    updated_at: string;
    object: Addon.Object_;
}

export namespace Addon {
    /** Catalog category. */
    export const Category = {
        Analytics: "analytics",
        Integrations: "integrations",
        Compliance: "compliance",
        Capacity: "capacity",
        Automation: "automation",
        Telematics: "telematics",
        Communications: "communications",
        Storage: "storage",
        Security: "security",
        Support: "support",
        Branding: "branding",
        WhiteLabel: "white_label",
        ApiAccess: "api_access",
        Onboarding: "onboarding",
        Other: "other",
    } as const;
    export type Category = (typeof Category)[keyof typeof Category];
    /** Pricing shape. */
    export const PricingModel = {
        Free: "free",
        Flat: "flat",
        PerSeat: "per_seat",
        Usage: "usage",
        Tiered: "tiered",
        OneTime: "one_time",
    } as const;
    export type PricingModel = (typeof PricingModel)[keyof typeof PricingModel];
    /** Billing cadence (null for free / one-time). */
    export const BillingPeriod = {
        Weekly: "weekly",
        Biweekly: "biweekly",
        Monthly: "monthly",
        Quarterly: "quarterly",
        Semiannual: "semiannual",
        Yearly: "yearly",
    } as const;
    export type BillingPeriod = (typeof BillingPeriod)[keyof typeof BillingPeriod];
    export const Object_ = {
        Addon: "addon",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
