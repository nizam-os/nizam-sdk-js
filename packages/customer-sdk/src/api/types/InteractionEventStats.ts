// This file was auto-generated by Fern from our API Definition.

import type * as NizamCustomer from "../index.js";

/**
 * Aggregated assistant interaction metrics over a window, metadata only (counts and token sums by interaction type); never conversation content.
 */
export interface InteractionEventStats {
    /** Start of the window (inclusive). */
    from: string;
    /** End of the window (exclusive). */
    to: string;
    /** Per-interaction-type counts and token sums; empty when the window had no interactions. */
    by_type: NizamCustomer.InteractionTypeBreakdown[];
    /** Object type discriminator. */
    object: InteractionEventStats.Object_;
}

export namespace InteractionEventStats {
    /** Object type discriminator. */
    export const Object_ = {
        InteractionEventStats: "interaction_event_stats",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
