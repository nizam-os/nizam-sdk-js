// This file was auto-generated by Fern from our API Definition.

/**
 * Rate an assistant reply. Submitting again records a new reaction rather than editing the old one — the change of mind is itself the interesting signal.
 */
export interface SubmitFeedbackRequest {
    /** The thumb. */
    rating: SubmitFeedbackRequest.Rating;
    /** Optional structured reason. Meaningful for either rating. */
    category?: SubmitFeedbackRequest.Category | undefined;
    /** Optional free-text note. Omit rather than sending an empty string. */
    comment?: string | undefined;
}

export namespace SubmitFeedbackRequest {
    /** The thumb. */
    export const Rating = {
        Up: "up",
        Down: "down",
    } as const;
    export type Rating = (typeof Rating)[keyof typeof Rating];
    /** Optional structured reason. Meaningful for either rating. */
    export const Category = {
        Inaccurate: "inaccurate",
        Unhelpful: "unhelpful",
        Unsafe: "unsafe",
        Formatting: "formatting",
        Other: "other",
    } as const;
    export type Category = (typeof Category)[keyof typeof Category];
}
