// This file was auto-generated by Fern from our API Definition.

import type * as NizamCarrier from "../index.js";

/**
 * The AI-proposed changes on one artifact, newest first. Check `stale` before offering Accept — a suggestion whose base version is no longer current can only be rejected.
 */
export interface SuggestedEditList {
    /** The artifact's suggested edits, newest first. */
    edits: NizamCarrier.SuggestedEdit[];
    object: SuggestedEditList.Object_;
}

export namespace SuggestedEditList {
    export const Object_ = {
        SuggestedEditList: "suggested_edit_list",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
