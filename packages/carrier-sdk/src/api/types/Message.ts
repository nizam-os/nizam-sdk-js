// This file was auto-generated by Fern from our API Definition.

import type * as NizamCarrier from "../index.js";

/**
 * One message on a conversation's active path, with its branch navigation.
 */
export interface Message {
    /** Message id. */
    id: string;
    /** Parent message id, or null for the root/first message. */
    parent_message_id?: string | undefined;
    /** Who authored this message. */
    role: Message.Role;
    /** The message body. */
    content: string;
    /** Creation timestamp. */
    created_at: string;
    /** Ids of every branch alternative at this point (siblings sharing the parent), creation-ordered and including this message. Switch to one via POST .../messages/{id}/switch-branch. */
    sibling_ids: string[];
    /** This message's 1-based position among its siblings. */
    sibling_index: number;
    /** Number of branch alternatives at this point (1 = no alternate branch). */
    sibling_count: number;
    /** The knowledge sources a grounded assistant reply cites, in presentation order — empty on user messages and on replies that did not ground. */
    citations: NizamCarrier.MessageCitation[];
    /** The files attached to a user message, in display order — empty on assistant messages and on turns sent without attachments. */
    attachments: NizamCarrier.MessageAttachment[];
    /** The turn's tool calls in request order, each with its approval-gate lifecycle state — empty on user messages and on tool-free replies. A pending call's id is the approval id the decision verbs address. */
    tool_calls: NizamCarrier.ToolCall[];
    /** The model that produced this reply — null on every message no model produced. */
    model?: string | undefined;
    usage?: NizamCarrier.MessageUsage | undefined;
    /** AI credits this reply's turn consumed — null whenever model is null, and 0 for a turn that was refunded or ran with no credit ceiling in force. */
    credits_charged?: number | undefined;
    /** The model's reasoning summary for this reply — null unless the organization retains reasoning and the model surfaced one. */
    reasoning?: string | undefined;
    /** Object type discriminator. */
    object: Message.Object_;
}

export namespace Message {
    /** Who authored this message. */
    export const Role = {
        User: "user",
        Assistant: "assistant",
        System: "system",
        Tool: "tool",
    } as const;
    export type Role = (typeof Role)[keyof typeof Role];
    /** Object type discriminator. */
    export const Object_ = {
        Message: "message",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
