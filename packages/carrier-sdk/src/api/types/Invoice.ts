// This file was auto-generated by Fern from our API Definition.

import type * as NizamCarrier from "../index.js";

/**
 * An accounts-receivable invoice: its billing party, money rollups, lifecycle status and dates.
 */
export interface Invoice {
    /** Unique identifier of the invoice. */
    id: string;
    /** The tenant-facing invoice number, when assigned. */
    invoice_number?: string | undefined;
    /** The kind of party billed. */
    billing_party_type: Invoice.BillingPartyType;
    /** Identifier of the billed party. */
    billing_party_id: string;
    /** Accounts-receivable lifecycle status. */
    status: Invoice.Status;
    subtotal: NizamCarrier.Money;
    discount: NizamCarrier.Money;
    tax: NizamCarrier.Money;
    amount_total: NizamCarrier.Money;
    amount_paid: NizamCarrier.Money;
    amount_due: NizamCarrier.Money;
    /** When the invoice was issued/finalized. */
    issued_at?: string | undefined;
    /** When payment is due. */
    due_at?: string | undefined;
    /** When the balance first reached zero. */
    paid_at?: string | undefined;
    /** Start of the billing period this invoice covers. */
    period_start?: string | undefined;
    /** End of the billing period this invoice covers. */
    period_end?: string | undefined;
    /** When the invoice record was created. */
    created_at: string;
    /** The invoice's line items (detail read only). */
    items?: NizamCarrier.InvoiceItem[] | undefined;
    object: Invoice.Object_;
}

export namespace Invoice {
    /** The kind of party billed. */
    export const BillingPartyType = {
        Merchant: "merchant",
        Customer: "customer",
        Carrier: "carrier",
        Organization: "organization",
    } as const;
    export type BillingPartyType = (typeof BillingPartyType)[keyof typeof BillingPartyType];
    /** Accounts-receivable lifecycle status. */
    export const Status = {
        Draft: "draft",
        Open: "open",
        PartiallyPaid: "partially_paid",
        PastDue: "past_due",
        Paid: "paid",
        Disputed: "disputed",
        Refunded: "refunded",
        Void: "void",
        Uncollectible: "uncollectible",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    export const Object_ = {
        Invoice: "invoice",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
