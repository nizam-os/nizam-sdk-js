// This file was auto-generated by Fern from our API Definition.

/**
 * An agent persona in the list view.
 */
export interface AgentProfileSummary {
    /** Stable persona id. */
    id: string;
    /** Display name. */
    name: string;
    /** Description. */
    description?: string | undefined;
    /** Reserved category. */
    category: AgentProfileSummary.Category;
    /** Whether platform-provisioned or org-authored. */
    source: AgentProfileSummary.Source;
    /** Lifecycle status. */
    status: AgentProfileSummary.Status;
    /** Whether this is the org's default persona. */
    is_default: boolean;
    /** The active version number. */
    active_version_number?: number | undefined;
    /** Creation time. */
    created_at: string;
    /** Last update time. */
    updated_at: string;
    object: AgentProfileSummary.Object_;
}

export namespace AgentProfileSummary {
    /** Reserved category. */
    export const Category = {
        CustomerSupport: "customer_support",
        SalesOutreach: "sales_outreach",
        CallCenter: "call_center",
        Operations: "operations",
        General: "general",
    } as const;
    export type Category = (typeof Category)[keyof typeof Category];
    /** Whether platform-provisioned or org-authored. */
    export const Source = {
        Builtin: "builtin",
        Custom: "custom",
    } as const;
    export type Source = (typeof Source)[keyof typeof Source];
    /** Lifecycle status. */
    export const Status = {
        Active: "active",
        Archived: "archived",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    export const Object_ = {
        AgentProfile: "agent_profile",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
