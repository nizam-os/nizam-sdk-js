// This file was auto-generated by Fern from our API Definition.

/**
 * A folder grouping related knowledge documents within an organization.
 */
export interface KnowledgeCollection {
    /** Stable UUID. */
    id: string;
    /** Display name (unique per organization, case-insensitive). */
    name: string;
    /** Optional description. */
    description?: string | undefined;
    /** Number of live documents filed under the collection. Returned in list projections; null on the single-collection read. */
    document_count?: number | undefined;
    /** When the collection was created. */
    created_at: string;
    /** When the collection was last modified. */
    updated_at: string;
    /** Object type discriminator. */
    object: KnowledgeCollection.Object_;
}

export namespace KnowledgeCollection {
    /** Object type discriminator. */
    export const Object_ = {
        KnowledgeCollection: "knowledge_collection",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
