// This file was auto-generated by Fern from our API Definition.

import type * as NizamCarrier from "../index.js";

/**
 * A credit note: the invoice it adjusts, its money rollups, reason, lifecycle status and dates.
 */
export interface CreditNote {
    /** Unique identifier of the credit note. */
    id: string;
    /** The invoice this credit note adjusts, when linked. */
    invoice_id?: string | undefined;
    /** The processor-assigned credit-note number, when assigned. */
    credit_note_number?: string | undefined;
    /** The kind of party credited. */
    billing_party_type: CreditNote.BillingPartyType;
    /** Identifier of the credited party. */
    billing_party_id: string;
    /** Credit-note lifecycle status. */
    status: CreditNote.Status;
    /** Why the credit note was issued, when supplied. */
    reason?: CreditNote.Reason | undefined;
    /** Whether the credit applied before or after payment, when supplied. */
    credit_type?: CreditNote.CreditType | undefined;
    subtotal: NizamCarrier.Money;
    tax: NizamCarrier.Money;
    total: NizamCarrier.Money;
    out_of_band: NizamCarrier.Money;
    /** Free-text memo, when supplied. */
    memo?: string | undefined;
    /** When the credit note took effect. */
    issued_at?: string | undefined;
    /** When the credit note was voided, if voided. */
    voided_at?: string | undefined;
    /** When the credit-note record was created. */
    created_at: string;
    /** The credit note's lines (detail read only). */
    lines?: NizamCarrier.CreditNoteLine[] | undefined;
    object: CreditNote.Object_;
}

export namespace CreditNote {
    /** The kind of party credited. */
    export const BillingPartyType = {
        Merchant: "merchant",
        Customer: "customer",
        Carrier: "carrier",
        Organization: "organization",
    } as const;
    export type BillingPartyType = (typeof BillingPartyType)[keyof typeof BillingPartyType];
    /** Credit-note lifecycle status. */
    export const Status = {
        Issued: "issued",
        Void: "void",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /** Why the credit note was issued, when supplied. */
    export const Reason = {
        Duplicate: "duplicate",
        Fraudulent: "fraudulent",
        OrderChange: "order_change",
        ProductUnsatisfactory: "product_unsatisfactory",
    } as const;
    export type Reason = (typeof Reason)[keyof typeof Reason];
    /** Whether the credit applied before or after payment, when supplied. */
    export const CreditType = {
        PrePayment: "pre_payment",
        PostPayment: "post_payment",
    } as const;
    export type CreditType = (typeof CreditType)[keyof typeof CreditType];
    export const Object_ = {
        CreditNote: "credit_note",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
