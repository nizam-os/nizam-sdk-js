// This file was auto-generated by Fern from our API Definition.

import type * as NizamCarrier from "../index.js";

/**
 * An agent persona with its active configuration version.
 */
export interface AgentProfile {
    /** Stable persona id. */
    id: string;
    /** Display name. */
    name: string;
    /** Description. */
    description?: string | undefined;
    /** Reserved category. */
    category: AgentProfile.Category;
    /** Whether platform-provisioned or org-authored. */
    source: AgentProfile.Source;
    /** Lifecycle status. */
    status: AgentProfile.Status;
    /** Whether this is the org's default persona. */
    is_default: boolean;
    active_version: NizamCarrier.AgentProfileVersion;
    /** Statuses this persona may legally transition to next. */
    allowed_transitions: AgentProfile.AllowedTransitions.Item[];
    /** Creation time. */
    created_at: string;
    /** Last update time. */
    updated_at: string;
    object: AgentProfile.Object_;
}

export namespace AgentProfile {
    /** Reserved category. */
    export const Category = {
        CustomerSupport: "customer_support",
        SalesOutreach: "sales_outreach",
        CallCenter: "call_center",
        Operations: "operations",
        General: "general",
    } as const;
    export type Category = (typeof Category)[keyof typeof Category];
    /** Whether platform-provisioned or org-authored. */
    export const Source = {
        Builtin: "builtin",
        Custom: "custom",
    } as const;
    export type Source = (typeof Source)[keyof typeof Source];
    /** Lifecycle status. */
    export const Status = {
        Active: "active",
        Archived: "archived",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    export type AllowedTransitions = AllowedTransitions.Item[];

    export namespace AllowedTransitions {
        /** Statuses this persona may legally transition to next. */
        export const Item = {
            Active: "active",
            Archived: "archived",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }

    export const Object_ = {
        AgentProfile: "agent_profile",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
