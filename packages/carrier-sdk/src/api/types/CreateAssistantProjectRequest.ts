// This file was auto-generated by Fern from our API Definition.

/**
 * Create a project to group conversations.
 */
export interface CreateAssistantProjectRequest {
    /** Display name. Unique per member, case-insensitively. */
    name: string;
    /** What the project collects. */
    description?: string | undefined;
    /** Standing brief every conversation filed into this project converses under. Applied below the persona's own instructions and above retrieved knowledge. */
    instructions?: string | undefined;
}
