// This file was auto-generated by Fern from our API Definition.

/**
 * A generated conversation transcript and its download grant.
 */
export interface ExportedTranscript {
    /** The stored transcript file's id. */
    file_id: string;
    /** The format rendered. */
    format: ExportedTranscript.Format;
    /** Short-lived presigned download URL. */
    download_url: string;
    /** When the download URL stops working. */
    expires_at: string;
    /** How many messages the transcript carries. */
    message_count: number;
    /** Size of the rendered file in bytes. */
    byte_size: number;
    /** Object type discriminator. */
    object: ExportedTranscript.Object_;
}

export namespace ExportedTranscript {
    /** The format rendered. */
    export const Format = {
        Markdown: "markdown",
        Json: "json",
    } as const;
    export type Format = (typeof Format)[keyof typeof Format];
    /** Object type discriminator. */
    export const Object_ = {
        ExportedTranscript: "exported_transcript",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
