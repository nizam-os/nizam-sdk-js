// This file was auto-generated by Fern from our API Definition.

/**
 * A manual fee / adjustment line to add to a settlement build.
 */
export interface MerchantPayoutLineInput {
    /** The contribution kind. */
    line_type: MerchantPayoutLineInput.LineType;
    /** The origin kind, for an entity-backed line. */
    source_type?: MerchantPayoutLineInput.SourceType | undefined;
    /** The originating entity id, when the line is entity-backed. */
    source_id?: string | undefined;
    /** Human-readable line description. */
    description?: string | undefined;
    /** Optional billing code. */
    code?: string | undefined;
    /** Signed amount in the payout's minor units (negative for a deduction). */
    amount?: number | undefined;
}

export namespace MerchantPayoutLineInput {
    /** The contribution kind. */
    export const LineType = {
        Source: "source",
        Tip: "tip",
        Fee: "fee",
        Tax: "tax",
        Surcharge: "surcharge",
        Discount: "discount",
        Adjustment: "adjustment",
        Credit: "credit",
        Reversal: "reversal",
        Rounding: "rounding",
    } as const;
    export type LineType = (typeof LineType)[keyof typeof LineType];
    /** The origin kind, for an entity-backed line. */
    export const SourceType = {
        CodCollection: "cod_collection",
        Order: "order",
        Task: "task",
        Parcel: "parcel",
        Adjustment: "adjustment",
        Fee: "fee",
        Credit: "credit",
    } as const;
    export type SourceType = (typeof SourceType)[keyof typeof SourceType];
}
