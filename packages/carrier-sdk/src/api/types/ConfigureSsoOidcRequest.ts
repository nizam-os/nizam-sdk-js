// This file was auto-generated by Fern from our API Definition.

import type * as NizamCarrier from "../index.js";

/**
 * Configure an OIDC connection from its discovery URL.
 */
export interface ConfigureSsoOidcRequest {
    /** The provider's discovery URL (`/.well-known/openid-configuration`). Endpoints are fetched and parsed server-side — never hand-typed. */
    discovery_url: string;
    /** Relying-party client id registered at the provider. */
    client_id: string;
    /** Relying-party client secret. WRITE-ONLY: stored in the identity system, never readable back. Omit on reconfigure to keep the existing secret; required on first configure. */
    client_secret?: string | undefined;
    /** Directory-group → role rules; replaces the stored set wholesale. */
    group_mappings?: NizamCarrier.SsoGroupMappingRequest[] | undefined;
    /** Role granted when no group matches; `operator` is not assignable. */
    fallback_role: ConfigureSsoOidcRequest.FallbackRole;
    /** Re-derive the role from groups on every sign-in (opt-in). */
    rederive_role_on_login?: boolean | undefined;
}

export namespace ConfigureSsoOidcRequest {
    /** Role granted when no group matches; `operator` is not assignable. */
    export const FallbackRole = {
        Admin: "admin",
        Dispatcher: "dispatcher",
        Manager: "manager",
        Staff: "staff",
        Viewer: "viewer",
        BillingAdmin: "billing_admin",
        Operator: "operator",
    } as const;
    export type FallbackRole = (typeof FallbackRole)[keyof typeof FallbackRole];
}
