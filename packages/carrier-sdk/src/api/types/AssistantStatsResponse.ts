// This file was auto-generated by Fern from our API Definition.

import type * as NizamCarrier from "../index.js";

export interface AssistantStatsResponse {
    /** Live (non-deleted) conversations. */
    conversation_count: number;
    /** Live (non-deleted) messages. */
    message_count: number;
    /** Window start (inclusive). */
    from: string;
    /** Window end (exclusive). */
    to: string;
    /** Per-interaction-type counts and token totals within the window. */
    interactions: NizamCarrier.InteractionTypeStatsResponse[];
    object: string;
}
