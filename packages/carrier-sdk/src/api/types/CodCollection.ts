// This file was auto-generated by Fern from our API Definition.

import type * as NizamCarrier from "../index.js";

/**
 * A cash-on-delivery collection: its refs, amount, lifecycle status and settlement link.
 */
export interface CodCollection {
    /** Unique identifier of the COD collection. */
    id: string;
    /** The merchant the collected cash is owed to. */
    merchant_id?: string | undefined;
    /** The delivery task (stop) the cash was collected against. */
    task_id?: string | undefined;
    /** The parcel the cash was collected against. */
    parcel_id?: string | undefined;
    /** The operator who collected the cash. */
    operator_id?: string | undefined;
    /** Lifecycle status. */
    status: CodCollection.Status;
    amount: NizamCarrier.Money;
    /** When the cash landed in the operator's hands. */
    collected_at?: string | undefined;
    /** The merchant payout this collection was reconciled into, once settled. */
    merchant_payout_id?: string | undefined;
    /** When the collection was reconciled into a payout. */
    reconciled_at?: string | undefined;
    /** When the COD was refunded to the customer. */
    refunded_at?: string | undefined;
    /** Free-form note recorded with the collection. */
    notes?: string | undefined;
    /** The lifecycle statuses this collection may legally move to next. */
    allowed_transitions: CodCollection.AllowedTransitions.Item[];
    /** When the collection record was created. */
    created_at: string;
    object: CodCollection.Object_;
}

export namespace CodCollection {
    /** Lifecycle status. */
    export const Status = {
        Pending: "pending",
        Collected: "collected",
        Deposited: "deposited",
        Disputed: "disputed",
        Reconciled: "reconciled",
        Refunded: "refunded",
        Canceled: "canceled",
        Failed: "failed",
        WrittenOff: "written_off",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    export type AllowedTransitions = AllowedTransitions.Item[];

    export namespace AllowedTransitions {
        export const Item = {
            Pending: "pending",
            Collected: "collected",
            Deposited: "deposited",
            Disputed: "disputed",
            Reconciled: "reconciled",
            Refunded: "refunded",
            Canceled: "canceled",
            Failed: "failed",
            WrittenOff: "written_off",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }

    export const Object_ = {
        CodCollection: "cod_collection",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
