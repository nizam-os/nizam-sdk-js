// This file was auto-generated by Fern from our API Definition.

import type * as NizamCarrier from "../index.js";

/**
 * Build a merchant-payout settlement statement.
 */
export interface BuildMerchantPayoutRequest {
    /** The merchant to settle. */
    merchant_id: string;
    /** Start of the settlement period (inclusive). */
    period_start?: string | undefined;
    /** End of the settlement period (inclusive). */
    period_end?: string | undefined;
    /** ISO-4217 currency code (the single currency the statement settles). */
    currency: string;
    /** The disbursement rail. */
    payment_method: BuildMerchantPayoutRequest.PaymentMethod;
    destination?: NizamCarrier.MerchantPayoutDestination | undefined;
    /** Manual fee / adjustment / credit lines to add (optional). */
    lines?: NizamCarrier.MerchantPayoutLineInput[] | undefined;
    /** Free-form settlement note. */
    notes?: string | undefined;
}

export namespace BuildMerchantPayoutRequest {
    /** The disbursement rail. */
    export const PaymentMethod = {
        Wire: "wire",
        Ach: "ach",
        Cheque: "cheque",
        ProcessorTransfer: "processor_transfer",
        Card: "card",
        Paypal: "paypal",
        DigitalWallet: "digital_wallet",
        Cash: "cash",
        InternalCredit: "internal_credit",
        Adjustment: "adjustment",
    } as const;
    export type PaymentMethod = (typeof PaymentMethod)[keyof typeof PaymentMethod];
}
