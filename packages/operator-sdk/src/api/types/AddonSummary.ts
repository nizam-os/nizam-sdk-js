// This file was auto-generated by Fern from our API Definition.

import type * as NizamOperatorRuntime from "../index.js";

/**
 * An add-on in the platform billing catalog (list view).
 */
export interface AddonSummary {
    /** Stable add-on id. */
    id: string;
    /** Stable add-on code (natural key). */
    code: string;
    /** Display name. */
    name: string;
    /** Catalog category. */
    category: AddonSummary.Category;
    /** Pricing shape. */
    pricing_model: AddonSummary.PricingModel;
    price?: NizamOperatorRuntime.Money | undefined;
    /** Billing cadence (null for free / one-time). */
    billing_period?: AddonSummary.BillingPeriod | undefined;
    /** Whether the add-on is sellable. */
    active: boolean;
    /** Whether a tenant may self-subscribe. */
    self_serve: boolean;
    /** Creation time. */
    created_at: string;
    object: AddonSummary.Object_;
}

export namespace AddonSummary {
    /** Catalog category. */
    export const Category = {
        Analytics: "analytics",
        Integrations: "integrations",
        Compliance: "compliance",
        Capacity: "capacity",
        Automation: "automation",
        Telematics: "telematics",
        Communications: "communications",
        Storage: "storage",
        Security: "security",
        Support: "support",
        Branding: "branding",
        WhiteLabel: "white_label",
        ApiAccess: "api_access",
        Onboarding: "onboarding",
        Other: "other",
    } as const;
    export type Category = (typeof Category)[keyof typeof Category];
    /** Pricing shape. */
    export const PricingModel = {
        Free: "free",
        Flat: "flat",
        PerSeat: "per_seat",
        Usage: "usage",
        Tiered: "tiered",
        OneTime: "one_time",
    } as const;
    export type PricingModel = (typeof PricingModel)[keyof typeof PricingModel];
    /** Billing cadence (null for free / one-time). */
    export const BillingPeriod = {
        Weekly: "weekly",
        Biweekly: "biweekly",
        Monthly: "monthly",
        Quarterly: "quarterly",
        Semiannual: "semiannual",
        Yearly: "yearly",
    } as const;
    export type BillingPeriod = (typeof BillingPeriod)[keyof typeof BillingPeriod];
    export const Object_ = {
        Addon: "addon",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
