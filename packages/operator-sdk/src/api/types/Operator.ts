// This file was auto-generated by Fern from our API Definition.

/**
 * A controlling agent: human, autonomous software, teleoperator, or hybrid.
 */
export interface Operator {
    /** Stable UUID. */
    id: string;
    /** Kind discriminator. */
    kind: Operator.Kind;
    /** Lifecycle status. */
    status: Operator.Status;
    /** Display name. */
    display_name?: string | undefined;
    /** Linked user id (humans with a Nizam login). Null for software agents. */
    user_id?: string | undefined;
    /** Legal entity accountable for this operator (required for autonomous/teleoperated). */
    responsible_party_id?: string | undefined;
    /** Owning organization id. */
    organization_id?: string | undefined;
    /** Software vendor. Part of an autonomous operator's IDENTITY, with `software_product` and `software_instance_id` — the triple is unique per organization and has no update path. Null on every other kind. */
    software_vendor?: string | undefined;
    /** Software product. Identity. Null on every other kind. */
    software_product?: string | undefined;
    /** The unit this stack runs on — the third identity part, required for autonomous operators. Null on every other kind. */
    software_instance_id?: string | undefined;
    /** The build currently running. STATE, not identity: an over-the-air update changes it in place via `PUT /v1/operators/{id}/software-version` and the operator keeps its machine credential, its assignments and its telemetry. Null on every other kind. */
    software_version?: string | undefined;
    /** Effective status of the operator's latest app invitation (driver enrolment): a pending invitation past its deadline reads `expired`. Null for operators that were never invited. */
    invite_status?: Operator.InviteStatus | undefined;
    /** When the operator's latest app invitation expires. Null for operators that were never invited. */
    invite_expires_at?: string | undefined;
    /** When the operator was created. */
    created_at: string;
    /** Object type discriminator. */
    object: Operator.Object_;
}

export namespace Operator {
    /** Kind discriminator. */
    export const Kind = {
        Human: "human",
        Autonomous: "autonomous",
        Teleoperated: "teleoperated",
        Hybrid: "hybrid",
    } as const;
    export type Kind = (typeof Kind)[keyof typeof Kind];
    /** Lifecycle status. */
    export const Status = {
        Offline: "offline",
        Available: "available",
        OnDuty: "on_duty",
        OnBreak: "on_break",
        Decommissioned: "decommissioned",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /** Effective status of the operator's latest app invitation (driver enrolment): a pending invitation past its deadline reads `expired`. Null for operators that were never invited. */
    export const InviteStatus = {
        Pending: "pending",
        Accepted: "accepted",
        Expired: "expired",
        Revoked: "revoked",
    } as const;
    export type InviteStatus = (typeof InviteStatus)[keyof typeof InviteStatus];
    /** Object type discriminator. */
    export const Object_ = {
        Operator: "operator",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
