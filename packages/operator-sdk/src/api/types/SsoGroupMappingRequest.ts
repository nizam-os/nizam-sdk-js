// This file was auto-generated by Fern from our API Definition.

/**
 * One directory-group → membership-role rule.
 */
export interface SsoGroupMappingRequest {
    /** Asserted group name, matched exactly. */
    group: string;
    /** Membership role the group grants. `operator` is not assignable through SSO — operators join through driver enrolment. */
    role: SsoGroupMappingRequest.Role;
}

export namespace SsoGroupMappingRequest {
    /** Membership role the group grants. `operator` is not assignable through SSO — operators join through driver enrolment. */
    export const Role = {
        Admin: "admin",
        Dispatcher: "dispatcher",
        Manager: "manager",
        Staff: "staff",
        Viewer: "viewer",
        BillingAdmin: "billing_admin",
        Operator: "operator",
    } as const;
    export type Role = (typeof Role)[keyof typeof Role];
}
