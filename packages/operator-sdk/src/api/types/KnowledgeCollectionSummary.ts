// This file was auto-generated by Fern from our API Definition.

/**
 * A lightweight reference to a collection — its id and display name — inlined into a document via `?expand[]=collection`.
 */
export interface KnowledgeCollectionSummary {
    /** Stable UUID. */
    id: string;
    /** Display name (unique per organization, case-insensitive). */
    name: string;
    /** Object type discriminator. */
    object: KnowledgeCollectionSummary.Object_;
}

export namespace KnowledgeCollectionSummary {
    /** Object type discriminator. */
    export const Object_ = {
        KnowledgeCollection: "knowledge_collection",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
