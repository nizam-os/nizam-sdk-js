// This file was auto-generated by Fern from our API Definition.

import type * as NizamOperatorRuntime from "../index.js";

/**
 * A single settlement line of a merchant payout: its kind, source, quantity and signed total.
 */
export interface MerchantPayoutLine {
    /** Unique identifier of the line. */
    id: string;
    /** The origin kind, or null for an ad-hoc charge line. */
    source_type?: string | undefined;
    /** The originating entity id, when the line is entity-backed. */
    source_id?: string | undefined;
    /** Human-readable line description. */
    description?: string | undefined;
    /** Optional billing code. */
    code?: string | undefined;
    /** The contribution kind. */
    line_type: string;
    /** Line quantity (fractional). */
    quantity: string;
    unit_amount: NizamOperatorRuntime.Money;
    amount_total: NizamOperatorRuntime.Money;
    object: MerchantPayoutLine.Object_;
}

export namespace MerchantPayoutLine {
    export const Object_ = {
        MerchantPayoutLine: "merchant_payout_line",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
