// This file was auto-generated by Fern from our API Definition.

import type * as NizamOperatorRuntime from "../index.js";

/**
 * Organization-scoped assistant configuration.
 */
export interface AssistantSettings {
    /** Whether the assistant is enabled for the organization. */
    enabled: boolean;
    /** Allowed persona (agent-profile) ids; empty = no restriction. */
    persona_allowlist: string[];
    /** Conversation retention in days; null = keep forever. */
    retention_days?: number | undefined;
    features: NizamOperatorRuntime.AssistantFeatureToggles;
    /** Org-level instructions prepended to every turn; null = none. */
    instructions?: string | undefined;
    /** Object type discriminator. */
    object: AssistantSettings.Object_;
}

export namespace AssistantSettings {
    /** Object type discriminator. */
    export const Object_ = {
        AssistantSettings: "assistant_settings",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
