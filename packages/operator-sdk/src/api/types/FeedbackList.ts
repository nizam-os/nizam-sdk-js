// This file was auto-generated by Fern from our API Definition.

import type * as NizamOperatorRuntime from "../index.js";

/**
 * Your current reactions across one conversation — one entry per message you have rated, so a client can render every thumb without a call per message.
 */
export interface FeedbackList {
    /** Your current reactions, one per rated message. */
    feedback: NizamOperatorRuntime.Feedback[];
    object: FeedbackList.Object_;
}

export namespace FeedbackList {
    export const Object_ = {
        FeedbackList: "feedback_list",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
