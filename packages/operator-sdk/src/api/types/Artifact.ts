// This file was auto-generated by Fern from our API Definition.

/**
 * A document, source file or diagram the assistant produced in a conversation, kept as a versioned, restorable history.
 */
export interface Artifact {
    /** Stable UUID. */
    id: string;
    /** The conversation this artifact was produced in. */
    conversation_id: string;
    /** What kind of thing this is, which selects how the client renders and edits it. */
    type: Artifact.Type;
    /** Display name. */
    title: string;
    /** Syntax hint for the editor — the source language of a `code` artifact or the dialect of a `diagram` one. Free-form; `null` for a `document`. */
    language?: string | undefined;
    /** The current version number. Versions are 1-based and dense; restoring an older version APPENDS a new one rather than moving this backwards. */
    current_version_number: number;
    /** When the artifact was first produced. */
    created_at: string;
    /** When the artifact last changed — a new version, a rename, or a language-hint change. This is the "last edited" a gallery shows. */
    updated_at: string;
    /** Object type discriminator. */
    object: Artifact.Object_;
}

export namespace Artifact {
    /** What kind of thing this is, which selects how the client renders and edits it. */
    export const Type = {
        Document: "document",
        Code: "code",
        Diagram: "diagram",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
    /** Object type discriminator. */
    export const Object_ = {
        Artifact: "artifact",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
