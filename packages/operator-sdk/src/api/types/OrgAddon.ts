// This file was auto-generated by Fern from our API Definition.

import type * as NizamOperatorRuntime from "../index.js";

/**
 * An organization's subscription to a catalog add-on.
 */
export interface OrgAddon {
    /** Stable org add-on id. */
    id: string;
    /** The subscribed add-on's id. */
    addon_id: string;
    /** The parent platform subscription this add-on attaches to. */
    platform_subscription_id?: string | undefined;
    /** Add-on lifecycle status. */
    status: OrgAddon.Status;
    /** Seat quantity (1 for flat / one-time; N for per-seat). */
    quantity: number;
    unit_price?: NizamOperatorRuntime.Money | undefined;
    /** When the add-on subscription began. */
    started_at?: string | undefined;
    /** Start of the current billing period. */
    current_period_start?: string | undefined;
    /** End of the current billing period. */
    current_period_end?: string | undefined;
    /** When the add-on was cancelled. */
    canceled_at?: string | undefined;
    /** Creation time. */
    created_at: string;
    /** Last update time. */
    updated_at: string;
    object: OrgAddon.Object_;
}

export namespace OrgAddon {
    /** Add-on lifecycle status. */
    export const Status = {
        Incomplete: "incomplete",
        IncompleteExpired: "incomplete_expired",
        Trialing: "trialing",
        Active: "active",
        PastDue: "past_due",
        Paused: "paused",
        Suspended: "suspended",
        Canceled: "canceled",
        Ended: "ended",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    export const Object_ = {
        OrgAddon: "org_addon",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
