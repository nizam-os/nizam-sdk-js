// This file was auto-generated by Fern from our API Definition.

import type * as NizamOperatorRuntime from "../index.js";

/**
 * A realized payment event: its owning invoice, rail, amount, refund state and status.
 */
export interface Payment {
    /** Unique identifier of the payment. */
    id: string;
    /** The invoice this payment settles, when invoice-owned. */
    invoice_id?: string | undefined;
    /** Realized lifecycle status. */
    status: Payment.Status;
    /** Settlement rail. */
    payment_method: Payment.PaymentMethod;
    amount: NizamOperatorRuntime.Money;
    refunded_amount: NizamOperatorRuntime.Money;
    /** How many refunds have been applied. */
    refund_count: number;
    /** When the funds were received. */
    received_at?: string | undefined;
    /** When the most recent refund was applied. */
    refunded_at?: string | undefined;
    /** When the payment record was created. */
    created_at: string;
    object: Payment.Object_;
}

export namespace Payment {
    /** Realized lifecycle status. */
    export const Status = {
        Pending: "pending",
        RequiresAction: "requires_action",
        Authorized: "authorized",
        Processing: "processing",
        Succeeded: "succeeded",
        PartiallyRefunded: "partially_refunded",
        Refunded: "refunded",
        Failed: "failed",
        Canceled: "canceled",
        Disputed: "disputed",
        ChargedBack: "charged_back",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /** Settlement rail. */
    export const PaymentMethod = {
        Card: "card",
        Ach: "ach",
        BankTransfer: "bank_transfer",
        DirectDebit: "direct_debit",
        Wire: "wire",
        Cheque: "cheque",
        Cash: "cash",
        Cod: "cod",
        Wallet: "wallet",
        AccountCredit: "account_credit",
        Adjustment: "adjustment",
    } as const;
    export type PaymentMethod = (typeof PaymentMethod)[keyof typeof PaymentMethod];
    export const Object_ = {
        Payment: "payment",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
