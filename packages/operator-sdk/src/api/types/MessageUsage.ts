// This file was auto-generated by Fern from our API Definition.

/**
 * Token usage of the model run that produced an assistant reply.
 */
export interface MessageUsage {
    /** Tokens in the prompt sent to the model (system prompt + conversation). */
    input_tokens?: number | undefined;
    /** Tokens the model generated in the reply. */
    output_tokens?: number | undefined;
    /** Tokens spent on hidden reasoning, when the provider reports them — 0 otherwise. */
    reasoning_tokens?: number | undefined;
    /** Prompt tokens served from the provider's context cache — 0 when uncached or unreported. */
    cached_tokens?: number | undefined;
}
