// This file was auto-generated by Fern from our API Definition.

/**
 * One model-proposed change to an artifact, awaiting a human decision. Offsets address the version the edit was computed against, which is why an edit goes stale once the artifact moves on.
 */
export interface SuggestedEdit {
    /** Stable UUID. */
    id: string;
    /** The shape of the change. */
    kind: SuggestedEdit.Kind;
    /** Start of the affected range: a 0-based character offset into the base version's content. */
    range_start: number;
    /** End of the affected range, exclusive. Equal to `range_start` for an `insert`, which is a zero-width insertion point. */
    range_end: number;
    /** The text to insert or substitute. `null` for a `delete`. */
    content?: string | undefined;
    /** One line on what the change improves, from the model. */
    rationale?: string | undefined;
    /** Whether the suggestion is still awaiting a decision, and how it was resolved. */
    status: SuggestedEdit.Status;
    /** The version applying this edit produced. Present only for `accepted` and `modified`. */
    resulting_version_id?: string | undefined;
    /** Who decided. `null` while `proposed`. */
    resolved_by?: string | undefined;
    /** When the decision was taken. `null` while `proposed`. */
    resolved_at?: string | undefined;
    /** When the suggestion was generated. */
    created_at: string;
    /** True once the artifact has changed since this edit was suggested, so its offsets no longer match the current content. A stale edit cannot be accepted or modified (409 `suggested_edit.stale`) but can still be rejected — generate suggestions again to review against the latest version. */
    stale: boolean;
    /** Object type discriminator. */
    object: SuggestedEdit.Object_;
}

export namespace SuggestedEdit {
    /** The shape of the change. */
    export const Kind = {
        Insert: "insert",
        Replace: "replace",
        Delete: "delete",
    } as const;
    export type Kind = (typeof Kind)[keyof typeof Kind];
    /** Whether the suggestion is still awaiting a decision, and how it was resolved. */
    export const Status = {
        Proposed: "proposed",
        Accepted: "accepted",
        Rejected: "rejected",
        Modified: "modified",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /** Object type discriminator. */
    export const Object_ = {
        SuggestedEdit: "suggested_edit",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
