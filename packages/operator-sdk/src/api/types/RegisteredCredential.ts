// This file was auto-generated by Fern from our API Definition.

/**
 * A sign-in method registered on the account.
 */
export interface RegisteredCredential {
    /** Keycloak's identifier for this credential. Opaque — pass it back verbatim to remove the credential; do not parse it. */
    id: string;
    /** Which kind of sign-in method this is. */
    kind: RegisteredCredential.Kind;
    /** The name the member gave it when registering. Null when they left it blank, or for kinds that are never named (the password, the recovery-code set). */
    name?: string | undefined;
    /** When it was registered. Null only for a credential migrated in from an older realm, which predates Keycloak recording the timestamp. */
    created_at?: string | undefined;
    /** Object type discriminator. */
    object: RegisteredCredential.Object_;
}

export namespace RegisteredCredential {
    /** Which kind of sign-in method this is. */
    export const Kind = {
        Password: "password",
        TwoStep: "two_step",
        RecoveryCodes: "recovery_codes",
        Passkey: "passkey",
    } as const;
    export type Kind = (typeof Kind)[keyof typeof Kind];
    /** Object type discriminator. */
    export const Object_ = {
        UserCredential: "user_credential",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
