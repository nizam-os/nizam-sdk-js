// This file was auto-generated by Fern from our API Definition.

/**
 * Registers a document, then upload its content via the file-upload API (`kind=knowledge_document`, `subject_type=knowledge_document`, `subject_id=`the returned document id). Once the upload completes and passes scanning, the document is extracted, chunked, and embedded.
 */
export interface CreateKnowledgeDocumentRequest {
    /** Display title. */
    title: string;
    /** Collection to file the document under, or null to leave it ungrouped. */
    collection_id?: string | undefined;
    /** Optional free-form metadata. */
    metadata?: Record<string, Record<string, unknown>> | undefined;
}
