// This file was auto-generated by Fern from our API Definition.

import type * as NizamOperatorRuntime from "../index.js";

/**
 * Configure a SAML 2.0 connection from its metadata (URL or uploaded document — exactly one).
 */
export interface ConfigureSsoSamlRequest {
    /** The IdP metadata document URL. Provide either this or `metadata_xml`, not both. */
    metadata_url?: string | undefined;
    /** The IdP metadata document itself, for providers that do not host one. Provide either this or `metadata_url`, not both. */
    metadata_xml?: string | undefined;
    /** Directory-group → role rules; replaces the stored set wholesale. */
    group_mappings?: NizamOperatorRuntime.SsoGroupMappingRequest[] | undefined;
    /** Role granted when no group matches; `operator` is not assignable. */
    fallback_role: ConfigureSsoSamlRequest.FallbackRole;
    /** Re-derive the role from groups on every sign-in (opt-in). */
    rederive_role_on_login?: boolean | undefined;
}

export namespace ConfigureSsoSamlRequest {
    /** Role granted when no group matches; `operator` is not assignable. */
    export const FallbackRole = {
        Admin: "admin",
        Dispatcher: "dispatcher",
        Manager: "manager",
        Staff: "staff",
        Viewer: "viewer",
        BillingAdmin: "billing_admin",
        Operator: "operator",
    } as const;
    export type FallbackRole = (typeof FallbackRole)[keyof typeof FallbackRole];
}
