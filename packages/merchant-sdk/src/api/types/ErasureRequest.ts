// This file was auto-generated by Fern from our API Definition.

/**
 * A subject-erasure request and what it destroyed.
 */
export interface ErasureRequest {
    /** Request id. */
    id: string;
    /** The organization whose data is erased. */
    organization_id: string;
    /** The data subject being erased. Retained here after the purge — this row is the evidence the obligation was discharged. */
    subject_id: string;
    /** Lifecycle state. */
    state: ErasureRequest.State;
    /** The legal hold currently deferring this request. Present only while `state` is `held`. */
    held_by_hold_id?: string | undefined;
    /** Rows removed per store. Every store reports, including ones that found nothing — a zero is evidence the store was visited. */
    purged_counts: Record<string, number>;
    /** Why the purge gave up. Present only while `state` is `failed`; carries the failure's shape, never conversation content. */
    failure_reason?: string | undefined;
    /** When the purge began. */
    started_at?: string | undefined;
    /** When it finished or failed. */
    completed_at?: string | undefined;
    /** When the request was raised. */
    created_at: string;
    object: ErasureRequest.Object_;
}

export namespace ErasureRequest {
    /** Lifecycle state. */
    export const State = {
        Pending: "pending",
        Held: "held",
        Purging: "purging",
        Completed: "completed",
        Failed: "failed",
    } as const;
    export type State = (typeof State)[keyof typeof State];
    export const Object_ = {
        AssistantErasureRequest: "assistant_erasure_request",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
