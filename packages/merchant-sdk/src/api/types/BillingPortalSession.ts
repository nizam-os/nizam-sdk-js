// This file was auto-generated by Fern from our API Definition.

/**
 * A short-lived Stripe-hosted billing portal session: the URL to redirect to and the return URL.
 */
export interface BillingPortalSession {
    /** The hosted billing portal URL to redirect the customer to. */
    url: string;
    /** Where the customer is returned after they finish in the portal. */
    return_url: string;
    /** When the session was created. */
    created_at: string;
    object: BillingPortalSession.Object_;
}

export namespace BillingPortalSession {
    export const Object_ = {
        BillingPortalSession: "billing_portal_session",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
