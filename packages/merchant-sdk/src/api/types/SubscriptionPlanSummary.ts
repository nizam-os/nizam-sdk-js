// This file was auto-generated by Fern from our API Definition.

import type * as NizamMerchant from "../index.js";

/**
 * A subscription plan in the platform catalog (list view).
 */
export interface SubscriptionPlanSummary {
    /** Stable plan id. */
    id: string;
    /** Stable plan code (natural key). */
    code: string;
    /** Display name. */
    name: string;
    /** Catalog lifecycle status. */
    status: SubscriptionPlanSummary.Status;
    /** Billing model (the price shape). */
    billing_model: SubscriptionPlanSummary.BillingModel;
    /** Billing cadence. */
    billing_period: SubscriptionPlanSummary.BillingPeriod;
    price: NizamMerchant.Money;
    /** Whether the plan is sellable. */
    active: boolean;
    /** Whether the plan shows on the public pricing page. */
    visible: boolean;
    /** Display order on the catalog / pricing page. */
    position: number;
    /** Creation time. */
    created_at: string;
    object: SubscriptionPlanSummary.Object_;
}

export namespace SubscriptionPlanSummary {
    /** Catalog lifecycle status. */
    export const Status = {
        Draft: "draft",
        Active: "active",
        Grandfathered: "grandfathered",
        Retired: "retired",
        Archived: "archived",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /** Billing model (the price shape). */
    export const BillingModel = {
        Free: "free",
        Flat: "flat",
        PerSeat: "per_seat",
        PerEvent: "per_event",
        Tiered: "tiered",
        Hybrid: "hybrid",
    } as const;
    export type BillingModel = (typeof BillingModel)[keyof typeof BillingModel];
    /** Billing cadence. */
    export const BillingPeriod = {
        Weekly: "weekly",
        Biweekly: "biweekly",
        Monthly: "monthly",
        Quarterly: "quarterly",
        Semiannual: "semiannual",
        Yearly: "yearly",
    } as const;
    export type BillingPeriod = (typeof BillingPeriod)[keyof typeof BillingPeriod];
    export const Object_ = {
        Plan: "plan",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
