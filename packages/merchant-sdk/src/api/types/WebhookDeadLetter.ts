// This file was auto-generated by Fern from our API Definition.

/**
 * An inbound webhook event whose processing exhausted its retries.
 */
export interface WebhookDeadLetter {
    /** Inbox row id. */
    id: string;
    /** The source provider. */
    provider: string;
    /** The provider's event id. */
    event_id: string;
    /** The event type that failed. */
    type: string;
    /** Number of processing attempts made. */
    attempt_count: number;
    /** Resolved owning organization, if processing got far enough to resolve it. */
    organization_id?: string | undefined;
    /** When the event was ingested (RFC 3339). */
    received_at: string;
    /** When it dead-lettered (RFC 3339). */
    failed_at?: string | undefined;
    /** The last processing error recorded. */
    error?: string | undefined;
    /** Object type discriminator. */
    object: WebhookDeadLetter.Object_;
}

export namespace WebhookDeadLetter {
    /** Object type discriminator. */
    export const Object_ = {
        WebhookDeadLetter: "webhook_dead_letter",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
