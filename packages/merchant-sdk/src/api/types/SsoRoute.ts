// This file was auto-generated by Fern from our API Definition.

/**
 * Whether an email domain federates, and the provider hint if so.
 */
export interface SsoRoute {
    /** True when the domain routes to an organization's SSO provider. */
    sso: boolean;
    /** The `idp_hint` to hand `/auth/login`; null when the domain does not federate. */
    idp_hint?: string | undefined;
    object: string;
}
