// This file was auto-generated by Fern from our API Definition.

/**
 * The autonomy envelope of an agent persona version.
 */
export interface AgentAutonomyPolicy {
    /** Whether this persona may run autonomously. */
    autonomous_allowed?: boolean | undefined;
    /** Cap on steps per run; absent for no cap. */
    max_steps?: number | undefined;
    /** Per-run token cap; absent for no cap. */
    token_budget?: number | undefined;
    /** Per-run AI-credit cap; absent for no cap. */
    credit_budget?: number | undefined;
    /** Whether tool calls need human approval. */
    tool_approval_mode?: AgentAutonomyPolicy.ToolApprovalMode | undefined;
    /** Allowed run triggers. */
    allowed_triggers?: AgentAutonomyPolicy.AllowedTriggers.Item[] | undefined;
}

export namespace AgentAutonomyPolicy {
    /** Whether tool calls need human approval. */
    export const ToolApprovalMode = {
        Auto: "auto",
        Manual: "manual",
    } as const;
    export type ToolApprovalMode = (typeof ToolApprovalMode)[keyof typeof ToolApprovalMode];
    export type AllowedTriggers = AllowedTriggers.Item[];

    export namespace AllowedTriggers {
        /** Allowed run triggers. */
        export const Item = {
            Manual: "manual",
            Cron: "cron",
            Event: "event",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }
}
