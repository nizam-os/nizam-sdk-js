// This file was auto-generated by Fern from our API Definition.

/**
 * An artifact produced in the conversation matched.
 */
export interface AssistantArtifactMatch {
    /** The artifact that matched. */
    artifact_id?: string | undefined;
    /** What kind of artifact it is — enough to badge the row. */
    artifact_type?: AssistantArtifactMatch.ArtifactType | undefined;
}

export namespace AssistantArtifactMatch {
    /** What kind of artifact it is — enough to badge the row. */
    export const ArtifactType = {
        Document: "document",
        Code: "code",
        Diagram: "diagram",
    } as const;
    export type ArtifactType = (typeof ArtifactType)[keyof typeof ArtifactType];
}
