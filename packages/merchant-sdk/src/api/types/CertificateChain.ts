// This file was auto-generated by Fern from our API Definition.

/**
 * The certificate authority chain, issuer-first.
 */
export interface CertificateChain {
    /** PEM-encoded CA certificates, issuer-first and ending at the root. Install these as the trust anchors for verifying the platform. */
    chain_pem: string[];
    /** Object type discriminator. */
    object: CertificateChain.Object_;
}

export namespace CertificateChain {
    /** Object type discriminator. */
    export const Object_ = {
        CertificateChain: "certificate_chain",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
