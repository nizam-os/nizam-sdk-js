// This file was auto-generated by Fern from our API Definition.

/**
 * Your current reaction to an assistant reply. Feedback is append-only, so this is the most recent of however many times you have rated this message.
 */
export interface Feedback {
    /** Stable UUID of the reaction. */
    id: string;
    /** The assistant message this is about. */
    message_id: string;
    /** The thumb. */
    rating: Feedback.Rating;
    /** Optional structured reason. Applies to either rating. */
    category?: Feedback.Category | undefined;
    /** Optional free-text note the member wrote. */
    comment?: string | undefined;
    /** When this reaction was submitted. */
    submitted_at: string;
    /** Object type discriminator. */
    object: Feedback.Object_;
}

export namespace Feedback {
    /** The thumb. */
    export const Rating = {
        Up: "up",
        Down: "down",
    } as const;
    export type Rating = (typeof Rating)[keyof typeof Rating];
    /** Optional structured reason. Applies to either rating. */
    export const Category = {
        Inaccurate: "inaccurate",
        Unhelpful: "unhelpful",
        Unsafe: "unsafe",
        Formatting: "formatting",
        Other: "other",
    } as const;
    export type Category = (typeof Category)[keyof typeof Category];
    /** Object type discriminator. */
    export const Object_ = {
        Feedback: "feedback",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
