// This file was auto-generated by Fern from our API Definition.

import type * as NizamMerchant from "../index.js";

/**
 * The knowledge documents a project's conversations are grounded on, and its capacity.
 */
export interface AssistantProjectContext {
    /** Attached documents, in the order they were attached. */
    documents: NizamMerchant.AssistantProjectDocument[];
    /** Prompt tokens the attached documents currently weigh. Documents still being indexed contribute nothing until their chunks land. */
    used_tokens: number;
    /** The project's context budget, in prompt tokens. */
    token_capacity: number;
    /** How many documents one project may attach. */
    max_documents: number;
    /** Object type discriminator. */
    object: AssistantProjectContext.Object_;
}

export namespace AssistantProjectContext {
    /** Object type discriminator. */
    export const Object_ = {
        AssistantProjectContext: "assistant_project_context",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
