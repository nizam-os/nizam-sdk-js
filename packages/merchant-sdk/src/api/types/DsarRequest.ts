// This file was auto-generated by Fern from our API Definition.

/**
 * A subject-access request and the bundle it produced.
 */
export interface DsarRequest {
    /** Request id. */
    id: string;
    /** The organization whose data is disclosed. */
    organization_id: string;
    /** The data subject the bundle is about. */
    subject_id: string;
    /** Lifecycle state. */
    state: DsarRequest.State;
    /** The stored bundle. Present only while `state` is `completed`. Its bytes are short-lived — request a download grant to fetch them, and expect a `410` once the retention window has lapsed. */
    bundle_file_id?: string | undefined;
    /** The bundle's size in bytes. */
    bundle_byte_size?: number | undefined;
    /** Rows disclosed per slice, plus the rendered transcript and artifact counts. Every slice reports, including ones that found nothing — a zero is evidence the store was visited. */
    slice_counts: Record<string, number>;
    /** Why assembly gave up. Present only while `state` is `failed`; carries the failure's shape, never conversation content. */
    failure_reason?: string | undefined;
    /** When assembly began. */
    started_at?: string | undefined;
    /** When it finished or failed. */
    completed_at?: string | undefined;
    /** When the request was raised. */
    created_at: string;
    object: DsarRequest.Object_;
}

export namespace DsarRequest {
    /** Lifecycle state. */
    export const State = {
        Pending: "pending",
        Building: "building",
        Completed: "completed",
        Failed: "failed",
    } as const;
    export type State = (typeof State)[keyof typeof State];
    export const Object_ = {
        AssistantDsarRequest: "assistant_dsar_request",
    } as const;
    export type Object_ = (typeof Object_)[keyof typeof Object_];
}
